apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-policy.fullname" . }}
  labels:
    {{- include "dclaw-policy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-policy.selectorLabels" . | nindent 4 }}
